# from transform import transform
# import polars as pl

# df = pl.DataFrame({
#     "amount": [10, -5],
#     "unit_price": [2, 3]
# })

# result = transform(df)

# assert result.shape[0] == 1
# assert "total_price" in result.columns
